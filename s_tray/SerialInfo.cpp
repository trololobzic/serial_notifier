#include "StdAfx.h"
#include "SerialInfo.h"

SerialInfo::SerialInfo(void)
{
}

SerialInfo::~SerialInfo(void)
{
}

/*
BOOL operator == (SerialInfo const & s1, SerialInfo const & s2) const
{
	return ((s1.port == s2.port) && (s1.name == s2.name));	
}

const BOOL operator != (SerialInfo const & s1, SerialInfo const & s2)
{
	return !(s1==s2);
}
*/